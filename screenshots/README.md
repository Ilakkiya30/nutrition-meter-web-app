# Screenshots

Place your app screenshots here for the GitHub README, for example:

- `dashboard.png` – Main dashboard view with summary cards
- `add-food.png` – The "Add Food" form
- `warning.png` – Calorie limit warning banner
- `mobile-view.png` – Responsive mobile layout

Git does not track empty folders, so this file keeps the `screenshots/`
folder in version control until you add real images.
